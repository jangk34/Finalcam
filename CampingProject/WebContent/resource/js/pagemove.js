const pageMove = {
	//	key : value,
		singleUrlParam : function(url,param){
			location.href="/"+url+"/"+param+".do"
		}
}
const homeMove = {
		aParam : function(url) {
			location.href="/"+url+".do"
		}  //  객체안의 함수(자바스크립트)지정으로 url이동 구현 //
//객체안의 url함수
}

